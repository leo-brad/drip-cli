export default function upgrade(...param) {
  //if (all) {
    //result = true;
  //} else {
    //result = await askQuestion(
      //'Are you need upgrade command drip ' + name + '.'
    //);
  //}
}
