const { exec, } = require('child_process');

exec('ls /', { maxBuffer: 10240 * 1024, }, (error, stdout, stderr) => {
  if (error) {
    throw error;
  } else {
    if (stdout) {
      console.log(stdout);
    } else {
      console.error(stderr);
    }
  }
});
